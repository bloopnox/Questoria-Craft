User:
  name: "Vaibhav19951"
  github: "Vaibhav19951"

runtime:
  image: node:20-alpine

commands:
  build:
    - npm install
  start: "node index.js"

repository:
  branch: "main"
  auto_deploy: true

environment:
  - BOT_TOKEN=${BOT_TOKEN}
